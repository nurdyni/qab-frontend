import React from 'react';
import FlexibleInput from './FlexibleInput.component';

const checkBox = () => {
  return (
    <div>
      <FlexibleInput type={'checkbox'} label={'Question'} />
    </div>
  );
};

export default checkBox;
